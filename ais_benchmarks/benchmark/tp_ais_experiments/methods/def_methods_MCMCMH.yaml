# Define the methods to be evaluated and its parameter configuration
methods:
    - name: mcmc_mh
      type: sampling_methods.CMetropolisHastings
      params:
        n_burnin: 10
        n_steps: 1
        kde_bw: 0.03
        proposal_sigma: 1.0 # Scaling parameter of the spherical gaussian MC proposal distribution

